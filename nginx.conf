server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri /index.html;
    }

    # Prevent caching of env-config.js
    location /env-config.js {
        add_header Cache-Control "no-cache";
    }
}
